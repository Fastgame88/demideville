(async()=>{await renderChrome();const site=await SITE;const slug=new URLSearchParams(location.search).get('slug');const s=(site.sections||[]).find(x=>x.slug===slug);if(!s){$('#sectionWrap').innerHTML='<h1>Section not found</h1>';return}document.title=`${s.title} — ${site.settings?.brand||'DEMI DEVILLE'}`;$('#sectionWrap').innerHTML=`<h1>${esc(s.title)}</h1>${s.image?`<img class="section-image" src="${esc(s.image)}" alt="${esc(s.title)}">`:''}<div class="section-content">${s.content||''}</div>`})();
